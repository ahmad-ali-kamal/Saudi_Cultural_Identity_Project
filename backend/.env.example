# MongoDB Configuration
# Copy this file to .env and update with your actual credentials

# ============================================================================
# OPTION 1: MongoDB Atlas (Cloud) - Recommended for Development & Production
# ============================================================================
# Uncomment and use this if you're using MongoDB Atlas
# Get your connection string from: Atlas Dashboard > Connect > Drivers
# MONGO_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/saudiculture?retryWrites=true&w=majority

# ============================================================================
# OPTION 2: Local Docker MongoDB - For Local Development
# ============================================================================
# Use these if running MongoDB via docker-compose locally
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=admin123
MONGO_USERNAME=admin
MONGO_PASSWORD=admin123
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DATABASE=saudiculture

# NOTE: If MONGO_URI is set, it will take precedence over individual settings above

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Server Configuration
SERVER_URL=http://localhost:8080

# AWS Cognito Configuration
AWS_COGNITO_USER_POOL_ID=your-user-pool-id
AWS_COGNITO_CLIENT_ID=your-app-client-id
AWS_COGNITO_CLIENT_SECRET=your-app-client-secret
AWS_COGNITO_REGION=your-region
